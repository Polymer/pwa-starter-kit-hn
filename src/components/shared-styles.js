export const sharedStyles = `
  a {
    color: inherit;
  }

  button {
    padding: 0;
    background: none;
    border: 0;
    color: inherit;
    cursor: pointer;
  }

  .reload-btn {
    border-bottom: 1px solid #000;
    font-size: 1.05em;
    cursor: pointer;
  }
`;
